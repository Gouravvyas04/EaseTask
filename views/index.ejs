<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Task Management</title>
</head>

<body class="bg-zinc-900 text-white">

    <div class="main w-full min-h-screen">
       
        <div class="form p-10 text-white max-w-xl mx-auto">
            <h2 class="text-3xl mb-6 text-center">Create a New Task</h2>
            <form action="/create" method="POST" class="space-y-6">
                <input class="block w-full rounded-md outline-none px-5 py-2 bg-zinc-800 text-white" type="text"
                    name="title" placeholder="Task Title" required>
                <textarea class="block w-full rounded-md mt-2 outline-none px-5 py-2 resize-none bg-zinc-800 text-white"
                    name="details" placeholder="Write the task details here..." required></textarea>
                <input class="w-full bg-blue-600 px-5 py-2 rounded-md text-white cursor-pointer" type="submit"
                    value="Create Task">
            </form>
        </div>

  
        <div class="tasks flex gap-6 flex-wrap p-10 max-w-7xl mx-auto">
            <% if (files.length> 0) { %>
                <% files.forEach(function(val) { %>
                    <div
                        class="task w-full md:w-1/3 lg:w-1/4 xl:w-1/5 p-4 bg-zinc-800 rounded-lg shadow-lg flex flex-col">
                        <h3 class="text-white text-2xl mb-2 truncate">
                            <%= val %>
                        </h3>
                        <div class="flex justify-between mt-2 items-center w-full">
                            <a class="text-blue-500 hover:underline" href="/file/<%= val %>">Read More..</a>
                            <a class="text-zinc-600" href="/edit/<%= val %>">Edit </a>
                            <a class="text-zinc-600" href="/delete/<%= val %>">Delete</a>
                        </div>
                    </div>
                    <% }) %>
                        <% } else { %>
                            <h3 class="text-zinc-600 text-center w-full">No Tasks Yet...</h3>
                            <% } %>
        </div>

    </div>

    <script src="/javascripts/script.js"></script>
</body>

</html>