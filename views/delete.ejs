<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete Task</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-zinc-900 text-white">

    <div class="container mx-auto p-10">
        <h1 class="text-3xl text-center mb-6">Delete Task</h1>

        <div class="max-w-md mx-auto bg-zinc-800 p-6 rounded-lg shadow-md">
            <p class="text-lg mb-4">
                Are you sure you want to delete the task:
                <span class="font-bold text-yellow-400">
                    <%= filename %>
                </span>?
            </p>
            <div class="flex justify-between">
                <a href="/" class="bg-gray-600 px-4 py-2 rounded-md text-white hover:bg-gray-700">Cancel</a>
                <form action="/delete" method="POST" class="inline-block">
                    <input type="hidden" name="filename" value="<%= filename %>">
                    <button type="submit"
                        class="bg-red-600 px-4 py-2 rounded-md text-white hover:bg-red-700">Delete</button>
                </form>
            </div>
        </div>
    </div>

</body>

</html>